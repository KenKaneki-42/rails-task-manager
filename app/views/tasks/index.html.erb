<div class="container-md">
  <h1 class="mb-3">Listing Tasks</h1>
  <div>
    <ul>
      <% @tasks.each do |task| %>
      <li><%= link_to task.title, task_path(task) %></li>
      <% end %>
    </ul>
  </div>
  <div>
    <p><%= link_to "Add a new task", new_task_path %></p>
  </div>
</div>
